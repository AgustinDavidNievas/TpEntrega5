package ar.edu.futbol5.inscripcion

interface CriterioInscripcion {
	
	def boolean dejasLugarAOtro()
	
	//Parece que esta interfaz esta medio al pedo, asi como esta solo esta haciendo que las subclases tengan el mismo tipo
}